spring:
  datasource:
    url: jdbc:h2:mem:campuscircle;MODE=MySQL;DATABASE_TO_LOWER=TRUE;NON_KEYWORDS=COMMENT;DB_CLOSE_DELAY=-1
    username: sa
    password:
    driver-class-name: org.h2.Driver

  sql:
    init:
      mode: always

campuscircle:
  view-count:
    flush-interval-seconds: 3600

logging:
  level:
    com.uninook.ai.PromptBuilder: DEBUG
